<h1>Ratings for {{restaurant}}</h1>

<a href="/">Home</a>
<a href="/addRestaurant">Add a restaurant</a>
<a href="/allRestaurants">All Restaurants</a>

<ul>
    {{#each ratings}}
    <li>
        Rating: {{this.rating}}
        comment: {{this.comment}}
    </li>
    {{/each}}
</ul>